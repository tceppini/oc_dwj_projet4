{% extends "layout.html.twig" %}

{% block title %}Connexion{% endblock %}

{% block content %}
    <h1> {{ block('title')}} </h1>

    {% if error %}
    <div class="alert alert-danger">
        <strong>Login failed!</strong> {{ error }}
    </div>
    {% endif %}

    <div class="container loginForm">
        <div class="row">
            <div class="col-sm-offset-2 col-sm-8">
                <form class="form-horizontal well" action="{{ path('login_check') }} " method="post">
                    <div class="form-group">
                        <label for="username" class="control col-xs-2">Identifiant</label>
                        <div class="col-xs-12">
                            <input type="text" class="form-control" id="username" name="_username" value="{{ last_username }}" placeholder="Entrez identifiant" required>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="password" class="control col-xs-4">Mot de passe</label>
                        <div class="col-xs-12">
                            <input type="password" class="form-control" id="password" name="_password" placeholder="Entrez mot de passe" required>
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-xs-offset-3 col-md-offset-5 col-xs-6">
                            <button type="submit" class="btn btn-primary"><span class="glyphicon glyphicon-log-in"></span> Se connecter</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}